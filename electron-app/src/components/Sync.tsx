import React from 'react';
import OptionButton from './OptionButton';

const Sync: React.FC = () => {
  return (
    <div className="syncWrapper base-component">
      <OptionButton type="sync" />
    </div>
  );
};

export default Sync;
